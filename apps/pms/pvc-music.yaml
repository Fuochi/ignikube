---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: music
  namespace: plex
spec:
  storageClassName: nfs-series
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
